<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.baserasumit.greet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EmployePage"></activity>
        <activity android:name=".StudentPage" />
        <activity android:name=".congratulationPage" />
        <activity android:name=".Congratulations" />
        <activity android:name=".employe" />
        <activity android:name=".student" />
        <activity android:name=".achivement" />
        <activity android:name=".AnniversaryPage" />
        <activity android:name=".Anniversary" />


        <activity android:name=".birthdayPage" />
        <activity android:name=".BIRTHDAY" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>